{
  "name": "blogyoo-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "migrate": "node migrate.js",
    "test": "jest"
  },
  "dependencies": {
    "argon2": "^0.41.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.1.1",
    "mysql2": "^3.2.0",
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "jest": "^30.4.2",
    "nodemon": "^3.0.2",
    "supertest": "^7.2.2"
  },
  "_comments": {
    "name": "Nom du package backend (identifiant du package)",
    "version": "Version du package - utile pour la gestion des releases",
    "private": "Champ true empêche la publication accidentelle sur npm",
    "scripts": {
      "dev": "Script d'exécution en développement — ici un placeholder",
      "migrate": "Exécute le runner de migration 'migrate.js'",
      "test": "Lance la suite de tests via 'jest'"
    },
    "dependencies": {
      "dotenv": "Charge les variables d'environnement depuis un .env",
      "mysql2": "Client MySQL utilisé par le runner de migrations"
    },
    "devDependencies": {
      "eslint": "Configuration et outils de lint",
      "jest": "Framework de tests unitaires"
    },
    "note": "Cette clé _comments est ajoutée pour documenter le fichier JSON sans casser npm. Ne modifiez pas les clés standard sans raison."
  }
}
